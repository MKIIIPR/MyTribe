@page "/creator/shop/products"
@using MudBlazor
@using Tribe.Services.States
@inject UserState UserState

<MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="mt-4">
    <AuthorizeView>
        <Authorized>
            @if (UserState?.TribeProfile?.IsCreator == true)
            {
                <Tribe.Ui.Pages.UserDashboard.CreatorShop.ProductsList />
            }
            else
            {
                <MudAlert Severity="Severity.Warning">
                    Sie müssen Creator sein, um Produkte zu verwalten.
                </MudAlert>
            }
        </Authorized>
        <NotAuthorized>
            <MudAlert Severity="Severity.Error">
                Bitte melden Sie sich an.
            </MudAlert>
        </NotAuthorized>
    </AuthorizeView>
</MudContainer>