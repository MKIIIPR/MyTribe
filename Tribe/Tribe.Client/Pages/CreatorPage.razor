@page "/{CreatorName}"
@page "/creator/{CreatorId}"

<Tribe.Ui.Pages.Creator.CreatorLandingPage CreatorId="@CreatorId" CreatorName="@CreatorName" />

@code {
    [Parameter]
    public string? CreatorId { get; set; }

    [Parameter]
    public string? CreatorName { get; set; }
}